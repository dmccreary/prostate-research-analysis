# TODO List

1. Create an updated python program that takes a PubMed ID and returns an abstract in a JSON file
2. Create a data profile of the existing "accepted" paper list that shows the range of dates
3. Create a program that creates a negative data set that matches the data profile of the accepted papers - avoid the "Clever Hans" problem
4. Put all the positive and negative papers in a single repository
5. Build a classification model on 80% of the data
6. Test the classification model on a testing data on the 20%
7. Provide a easy-to-use service that will classify new papers every year
8. Add the positive hit to the official website